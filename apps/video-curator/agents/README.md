# Video Curator — Agent docs

Per-tool instructions for Cursor agents. Monorepo-wide docs: `agents/workspace/` (repo root).

Add feature/spec docs in this folder as needed. After changing Video Curator behavior, update the matching doc here.
