{
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.9 --activate && cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm build --filter video-curator",
  "outputDirectory": "dist",
  "build": {
    "env": {
      "ENABLE_EXPERIMENTAL_COREPACK": "1"
    }
  }
}
